"use client"

import { url } from "inspector"
import { title } from "process"

const slides = {
    {
        id: 1,
        title: "Summmer Sale Sollections",
        description: "Sale! Up to 50% off!",
        img: "",
        url: "/",
        bg: "bg-gradient-to-r from-yellow-50 to-pink-50",
    }
}

const Homepage = () => {
    return (
        <div>

        </div>
    )
}

export default Homepage